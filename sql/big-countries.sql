-- to get multiple keys in the result use commas like this
SELECT name, population, area

FROM world

-- use greater than and equal syntax these values
WHERE area >= '3000000' OR population >= '25000000'
